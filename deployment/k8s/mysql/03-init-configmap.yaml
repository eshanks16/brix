apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init
  namespace: brix
data:
  # Empty init script - let the application handle all migrations
  # This prevents schema conflicts between MySQL init and app migrations
  init.sql: |
    -- Database initialization is handled by the application's migration system
    -- See internal/database/database.go for migration details
