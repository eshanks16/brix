apiVersion: v1
kind: Service
metadata:
  name: brix-pizza
  namespace: brix
  labels:
    app: brix-pizza
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: brix-pizza
  sessionAffinity: None
